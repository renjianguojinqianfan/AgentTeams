# servevo 侵入记录（servevo/ 外改动登记）

> 依据 ADR-003：零侵入是**默认策略 + 可突破例外**。任何 `AgentTeams/` 下 servevo/ 以外的官方文件改动，必须在此登记并通过 verify 核对，否则视为违规。
> 规矩：优先走官方扩展面（copaw `--package` / `spec.env` / CRD / 插件平台 / env 注入），仅在扩展面不足时才改官方核心源码；改动须收敛到最小、记录、答辩主动声明。

## 登记表

| 日期 | 文件/路径 | 改动内容 | 理由 | 走扩展面还是改核心 | 回滚方式 |
|---|---|---|---|---|---|
| — | （暂无） | — | — | — | — |

> 当前 servevo/ 外无改动。后续任何 servevo/ 外改动先在此登记，再改代码，否则 `make verify` 拒绝通过。