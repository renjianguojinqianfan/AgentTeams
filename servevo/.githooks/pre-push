#!/bin/sh
# pre-push: servevo 全量门禁（~30s）—— 4 模块单测 + 密钥枯死 + 零侵入核对
# 失败阻止 push；这是代码离开本地前的最后自查（对应 interview-agent pre-push）
powershell.exe -NoProfile -Command "bash servevo/verify.sh; if (\$LASTEXITCODE -ne 0) { Write-Host 'servevo 全量闸门失败，拒绝推送' -ForegroundColor Red; exit 1 }; Write-Host 'servevo 全量闸门通过 - 允许推送' -ForegroundColor Green"