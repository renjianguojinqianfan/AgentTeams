[project]
name = "servevo-obs"
version = "0.1.0"
description = "Servevo 可观测：AgentLoop/CMS OTLP 同契约上报 + 自建等价观测（Trace/Metrics/Log）"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
servevo-obs = "servevo_obs.cli:main"

[tool.setuptools.packages.find]
include = ["servevo_obs*"]